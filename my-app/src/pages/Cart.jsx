import { useNavigate } from "react-router-dom"

export default function Cart({cart}) {

    const navigate = useNavigate()
    
    return (
        <>
            {/* {cart.map(games) => (

            )} */}
        </>
    )
}